# Prompts

`python -m warm_company prompts` writes:

- `MASTER_STYLE.md` — frozen prefix
- `library.json` — exact prompt string for every trait × class that exists in config

Submit the `prompt` field of a library row to Grok Image. Do not freelance a new prefix.
